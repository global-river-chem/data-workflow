## Custom Functions & Repeated Operations

The scripts in this folder each include some custom function or flexible chunk of code that can be iteratively re-run. The purpose of these scripts is to reduce duplication within 'actual' workflow scripts and create single, centralized, sources of truth for operations with narrow scopes/limited accepted inputs/outputs.

See the comments within each script for a description of the purpose of that tool.
