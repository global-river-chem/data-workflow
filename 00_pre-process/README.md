## Data Pre-Processing

The 'actual' data standardization workflow is built to be as generic and flexible as possible, however, data are sometimes so idiosyncratic or malformed that the standardization workflow can't operate on them as-is. In such edge cases, pre-processing is performed by the scripts here.
